# قائمة الأمان - أرزاق

## قبل كل نشر

- تشغيل `npm run security:audit` والتأكد من عدم وجود ثغرات high أو critical.
- تشغيل `npx tsc --noEmit` بدون أخطاء.
- تشغيل `npm run lint` بدون أخطاء.
- التأكد من ضبط جميع متغيرات البيئة في Vercel/الاستضافة.
- التأكد أن `NEXTAUTH_SECRET` عشوائي وطوله 32 حرفاً على الأقل.
- التأكد أن Rate Limiting متصل ويعمل قبل فتح النظام للعامة.

## بعد كل نشر

- اختبار تسجيل الدخول والخروج.
- اختبار إنشاء حساب جديد.
- اختبار نشر طلب.
- اختبار تقديم عرض.
- اختبار رفع صورة.
- فحص Security Headers عبر securityheaders.com.
- فحص SSL عبر ssllabs.com.

## شهرياً

- مراجعة Audit Logs للأنشطة المشبوهة.
- مراجعة محاولات تسجيل الدخول الفاشلة.
- تحديث المكتبات بعد مراجعة `npm audit`.
- مراجعة المستخدمين المحظورين.

## في حالة اختراق

1. تعطيل المنصة مؤقتاً من لوحة الاستضافة.
2. تدوير `NEXTAUTH_SECRET` فوراً.
3. تدوير بيانات اتصال قاعدة البيانات.
4. تدوير مفاتيح Cloudflare R2.
5. مراجعة Audit Logs لآخر 24 ساعة.
6. إبلاغ المستخدمين المتأثرين عند الحاجة.

## قواعد ذهبية

- لا نثق بأي مدخلات من العميل: Zod ثم sanitize ثم الحفظ.
- لا نعرض stack traces أو تفاصيل داخلية للمستخدم.
- لا نسجل كلمات المرور أو OTP أو tokens.
- لا نعيد `passwordHash` في أي استجابة.
- لا نستخدم string concatenation في SQL.
- الصلاحيات تفشل بالإغلاق: المنع هو الافتراضي.
