# قائمة تجهيز الإطلاق - أرزاق

هذه القائمة تستخدم قبل إطلاق النسخة التجريبية أو الإنتاجية من منصة أرزاق.

## 1. البيئة والاستضافة

- [ ] إنشاء مشروع Vercel وربطه بالمستودع.
- [ ] ضبط `NEXT_PUBLIC_SITE_URL` على رابط الإنتاج.
- [ ] ضبط `NEXTAUTH_URL` و `AUTH_URL` على رابط الإنتاج.
- [ ] توليد `NEXTAUTH_SECRET` عشوائي بطول 32 حرفاً أو أكثر.
- [ ] تفعيل HTTPS فقط.
- [ ] ربط الدومين الرسمي عند توفره.

## 2. قاعدة البيانات

- [ ] استخدام Neon branch مخصص للإنتاج.
- [ ] ضبط `DATABASE_URL` كرابط pooled.
- [ ] ضبط `DATABASE_URL_UNPOOLED` كرابط direct للمigrations.
- [ ] تشغيل:

```bash
npm run db:deploy
npm run db:generate
```

- [ ] التأكد من وجود حساب أدمن.
- [ ] عدم استخدام بيانات seed التجريبية في الإنتاج العام.

## 3. التخزين والملفات

- [ ] إنشاء bucket في Cloudflare R2.
- [ ] تفعيل public access أو custom domain للصور.
- [ ] ضبط CORS للواجهة الرسمية فقط.
- [ ] ضبط مفاتيح R2 في Vercel.
- [ ] اختبار رفع صورة بروفايل وحذف/استبدال الصورة القديمة.

## 4. البريد الإلكتروني

- [ ] في التطوير: Gmail SMTP مع App Password.
- [ ] في الإنتاج: ربط دومين رسمي واستخدام Resend أو SMTP رسمي.
- [ ] اختبار إرسال رمز توثيق البريد.
- [ ] اختبار إرسال رمز إعادة تعيين كلمة المرور.
- [ ] التأكد أن `EMAIL_FROM` يستخدم بريد موثوق.

## 5. الأمان

- [ ] تشغيل:

```bash
npx tsc --noEmit
npm run lint
npm run build
npm run test:e2e:smoke
npm run security:audit
```

- [ ] مراجعة `SECURITY.md`.
- [ ] التأكد من تفعيل Rate Limiting.
- [ ] التأكد من عدم وجود أسرار داخل GitHub.
- [ ] تدوير أي مفاتيح تم مشاركتها أثناء التطوير.
- [ ] فحص Security Headers بعد النشر.

## 6. تجربة المستخدم

- [ ] اختبار التسجيل كصاحب طلب.
- [ ] اختبار التسجيل كمقدم خدمة.
- [ ] اختبار توثيق البريد.
- [ ] اختبار نشر طلب ودخوله مرحلة قيد المراجعة.
- [ ] اختبار موافقة الأدمن على الطلب.
- [ ] اختبار تقديم عرض وقبوله.
- [ ] اختبار إخفاء واتساب صاحب الطلب قبل قبول العرض.
- [ ] اختبار ظهور واتساب صاحب الطلب فقط لمقدم الخدمة المقبول.
- [ ] اختبار تحكم مقدم الخدمة في زر واتساب العام.
- [ ] اختبار صفحة مقدمي الخدمات والبروفايل العام.
- [ ] اختبار الموبايل على شبكة محلية وجهاز حقيقي.

## 7. الإدارة والتشغيل

- [ ] اختبار تسجيل دخول الأدمن من `/admin/login`.
- [ ] اختبار إضافة أدمن جديد.
- [ ] اختبار مراجعة الطلبات.
- [ ] اختبار طلب تعديل طلب وإرسال إشعار للعميل.
- [ ] اختبار توثيق مقدم خدمة من لوحة الإدارة.
- [ ] مراجعة Audit Logs.

## 8. قرار الإطلاق

لا يتم الإطلاق إلا إذا كانت البنود التالية ناجحة:

- [ ] build ناجح.
- [ ] smoke test ناجح.
- [ ] لا توجد أخطاء TypeScript.
- [ ] لا توجد أخطاء ESLint.
- [ ] المسار الكامل يعمل من التسجيل حتى قبول العرض.
- [ ] البريد يعمل.
- [ ] التخزين يعمل.
- [ ] الأدمن يستطيع إدارة الطلبات والمستخدمين.
